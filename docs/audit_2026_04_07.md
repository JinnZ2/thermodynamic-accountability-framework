# TAF Codebase Audit — 2026-04-07

## Compilation Status

| File | Status |
|---|---|
| `core/fatigue_model.py` | PASS |
| `core/human_system_collapse_model.py` | PASS |
| `core/data_logger.py` | PASS |
| `core/heat_leak_case.py` | PASS |
| `core/automation_assessment.py` | PASS |
| `core/integrations/haas_fieldlink.py` | PASS |
| `core/integrations/ferret_fieldlink.py` | PASS |
| `simulations/lhri_sim.py` | PASS |
| `simulations/seed_sim.py` | PASS |
| `core/atbs/functional_detector.py` | FAIL — smart quotes |
| `core/atbs/test_v2.py` | FAIL — smart quotes |
| `simulations/full_coupled_system.py` | FAIL — smart quotes |
| `simulations/federation.py` | FAIL — smart quotes |
| `simulations/emergent_federation.py` | FAIL — smart quotes |
| `simulations/node_v3_ipi.py` | FAIL — smart quotes |
| `game_theory/proof-pipeline.py` | FAIL — smart quotes |
| `game_theory/proof-pipelineV2.py` | FAIL — smart quotes |

**9 files compile clean. 8 files fail.**

---

## CRITICAL — Won't Run

### 1. Smart Quotes in Python Files (8 files)

Unicode curly quotes (`U+201C` `"`, `U+201D` `"`) appear throughout 8 files, causing `SyntaxError: invalid character` on import.

**Affected files:**
- `core/atbs/functional_detector.py`
- `core/atbs/test_v2.py`
- `simulations/full_coupled_system.py`
- `simulations/federation.py`
- `simulations/emergent_federation.py`
- `simulations/node_v3_ipi.py`
- `game_theory/proof-pipeline.py`
- `game_theory/proof-pipelineV2.py`

**Fix:** Replace all `"` `"` with ASCII `"`, and `'` `'` with `'`.

### 2. Markdown Code Fences in Python (8 files)

Triple backtick fences (` ``` `) appear inside Python source, from copy-paste out of markdown. Code inside these blocks is unreachable — Python treats ` ``` ` as a syntax error or a string literal depending on context.

**Same 8 files as above.**

**Fix:** Remove all ` ``` ` lines.

### 3. Mangled Dunder Methods (5 files)

`__init__`, `__dict__` appear as `**init**`, `**dict**` — markdown bold formatting leaked into Python source.

**Affected files and examples:**
- `simulations/full_coupled_system.py`: `n.**dict**` instead of `n.__dict__`
- `simulations/federation.py`: `FedNode(**n.**dict**)` instead of `FedNode(**n.__dict__)`
- `simulations/node_v3_ipi.py`: `s.**dict**` instead of `s.__dict__`
- `game_theory/proof-pipeline.py`: `def **init**(self):` instead of `def __init__(self):`
- `game_theory/proof-pipelineV2.py`: same pattern

**Fix:** Replace `**init**` → `__init__`, `**dict**` → `__dict__`, etc.

---

## HIGH — Logic / Design Bugs

### 4. Dataclass Default Evaluated at Class Time (`simulations/seed_sim.py`)

```python
@dataclass
class Community:
    K_kinesthetic: float = np.random.uniform(0.5, 0.9)  # line 13
    K_temporal:    float = np.random.uniform(0.5, 0.9)  # line 14
    ...
```

`np.random.uniform()` is called **once** when the class is defined. Every `Community()` instance that doesn't override these fields gets the **same** value. All communities start identical.

**Fix:** Use `field(default_factory=lambda: np.random.uniform(0.5, 0.9))` or initialize in `__post_init__`.

### 5. Duplicate `HumanSystemModel` Class

Defined independently in both:
- `core/fatigue_model.py` (line 142) — delegates to `FatigueModel`
- `core/human_system_collapse_model.py` (line 4) — standalone implementation

Different method signatures (`compute_collapse_risk` vs `assess_system`), different return types (tuple vs dict). Which one should be used? The CLAUDE.md audit notes from 2026-03-24 say `HumanSystemModel` now delegates to `FatigueModel` (DRY), but the standalone version in `human_system_collapse_model.py` still exists.

### 6. Division by Zero Risk

- `simulations/full_coupled_system.py` ~line 119: `(val-floor)/(1-floor)` — crashes if `floor == 1.0`
- `simulations/federation.py` ~line 156: `3.0/n_nodes` — crashes if `n_nodes == 0`

### 7. Missing Method Call Parentheses (`core/atbs/functional_detector.py`)

```python
datetime.now() - max(breaches, key=lambda i: i.timestamp).timestamp
```

Should be `.timestamp()` (with parens) — without them, this subtracts a method object from a datetime, raising `TypeError`.

---

## MEDIUM — Quality / Convention

### 8. Naming Convention Violations (`simulations/full_coupled_system.py`)

Per CLAUDE.md, functions use `snake_case`. Found:
- `IPI_f(n)` — should be `ipi_f(n)` or `compute_ipi(n)`
- `AI_s(n)` — should be `ai_s(n)` or `compute_ai_score(n)`
- `K_prot(n)` — `K_` prefix reserved for knowledge dimensions

### 9. Stub Modules

`core/heat_leak_case.py` (32 lines) and `core/data_logger.py` (23 lines) are minimal stubs — not fully realized implementations. They work but cover only basic scenarios.

### 10. `core/automation_assessment.py` Uses `input()` at Module Level

`run_example()` calls `input()` interactively. This is fine for the `__main__` guard, but the function itself is tightly coupled to stdin. Not usable programmatically without the interactive prompt.

---

## Summary

| Severity | Count | Description |
|---|---|---|
| CRITICAL | 3 classes | Smart quotes, markdown fences, mangled dunders — 8 files won't compile |
| HIGH | 4 issues | Dataclass default bug, duplicate class, div/zero, missing parens |
| MEDIUM | 3 issues | Naming conventions, stub modules, interactive coupling |

### Priority Fix Order

1. Smart quotes + markdown fences + dunder names (fixes 8 files at once)
2. `seed_sim.py` dataclass defaults
3. Consolidate or remove duplicate `HumanSystemModel`
4. Division by zero guards
5. Method call parentheses fix
